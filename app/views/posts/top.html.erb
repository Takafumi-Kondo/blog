<div class="container">
	<div class="row">
		<div class="col-xs-10 col-xs-offset-1">
			<div class="row row-eq-height">
				<% @posts_pv_count.each do |post| %>
					<div class="col-xs-2 post_contents">
						<%= attachment_image_tag post, :post_headerimage, format: 'jpeg', fallback: 'no_image.jpg', class: 'post_headerimage' %>
						<%= link_to post_path(post.id), :style=> 'text-decoration: none;' do %>
							<h4><%= post.title.truncate(20) %></h4>
						<% end %>
					</div>
				<% end %>
			</div>
		</div>
<!-- ジャンル -->
		<div class="col-xs-3">
			<h3>
				<% @genre_popular_posts.first(1).each do |post| %>
					<%= post.genre.genre_name %>
				<% end %>
			</h3>
			<% @genre_popular_posts.each do |post| %>
				<div class="row genre-content">
					<div class="col-xs-5 col-xs-offset-1">
						<%= attachment_image_tag post, :post_headerimage, format: 'jpeg', fallback: 'no_image.jpg', class: 'post_headerimage' %>
					</div>
					<%= link_to post_path(post.id), :style=> 'text-decoration: none;' do %>
						<h4><%= post.title.truncate(34) %></h4>
					<% end %>
				</div>
			<% end %>
		</div>
		<div class="col-xs-3">
			<h3>
				<% @genre_popular_posts2.first(1).each do |post| %>
					<%= post.genre.genre_name %>
				<% end %>
			</h3>
			<% @genre_popular_posts2.each do |post| %>
				<div class="row genre-content">
					<div class="col-xs-5 col-xs-offset-1">
						<%= attachment_image_tag post, :post_headerimage, format: 'jpeg', fallback: 'no_image.jpg', class: 'post_headerimage' %>
					</div>
					<%= link_to post_path(post.id), :style=> 'text-decoration: none;' do %>
						<h4><%= post.title.truncate(34) %></h4>
					<% end %>
				</div>
			<% end %>
		</div>
		<div class="col-xs-3">
			<h3>
				<% @genre_popular_posts3.first(1).each do |post| %>
					<%= post.genre.genre_name %>
				<% end %>
			</h3>
			<% @genre_popular_posts3.each do |post| %>
				<div class="row genre-content">
					<div class="col-xs-5 col-xs-offset-1">
						<%= attachment_image_tag post, :post_headerimage, format: 'jpeg', fallback: 'no_image.jpg', class: 'post_headerimage' %>
					</div>
					<%= link_to post_path(post.id), :style=> 'text-decoration: none;' do %>
						<h4><%= post.title.truncate(34) %></h4>
					<% end %>
				</div>
			<% end %>
		</div>

		<div class="row">
		<!-- 新着記事 -->
			<section class="col">
			</section>
		<!-- 人気ユーザー -->
			<section class="col">
			</section>
		</div>
	</div>
</div>
